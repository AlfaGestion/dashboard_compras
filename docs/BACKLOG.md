# Backlog — Dashboard de Compras · Alfa Gestión

Ítems pendientes ordenados por prioridad. Mover a CHANGELOG.md cuando se implementen.

---

## Alta prioridad

- [ ] **Sort numérico en tabla de resultado principal**: actualmente ordena como texto; columnas numéricas deberían ordenar por valor (igual que la tabla agrupada).
- [ ] **Exportar tabla agrupada a Excel**: hoy el botón Excel descarga el resultado original, no el resultado del agrupador dinámico.

---

## Media prioridad

- [ ] **Manual `docs/manual_consultas.md`**: copiar (o enlazar) el manual embebido a la carpeta raíz `docs/` para que sea legible directamente desde el repositorio.
- [ ] **Agrupador persistente entre ejecuciones**: opcionalmente conservar la configuración de agrupación al re-ejecutar con parámetros distintos.
- [ ] **Diseñador de Consultas en el Launcher**: el badge "Próximamente" en la pantalla principal puede activarse ahora que el módulo está completo.
- [ ] **Paginación en tabla agrupada**: si hay muchos grupos, mostrar paginación como en la tabla principal.

---

## Baja prioridad / Ideas

- [ ] Guardar consultas favoritas por usuario (localStorage).
- [ ] Columna de última ejecución en el árbol de consultas.
- [ ] Vista previa del SQL al pasar el cursor por un nodo del árbol.
- [ ] Módulo Ventas (estructura similar a Compras).
- [ ] Módulo Stock.
